import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.glow.businessboard',
  appName: 'Glow Business Board',
  webDir: 'dist/client'
};

export default config;
